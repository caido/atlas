import { Classic } from '@caido/primevue';

/* Re-exporting the Classic theme from @caido/primevue 
 * This is necessary because @primevue/nuxt-module only works with the default export
 */
export default Classic; 